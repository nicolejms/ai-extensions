---
"radius": patch
---

Use the common Radius React graph renderer in the self-contained Canvas artifact while preserving Canvas source-opening, deployment, and branch behavior. Isolate simultaneous graph details and report degraded layouts instead of overlapping invalid positions.
