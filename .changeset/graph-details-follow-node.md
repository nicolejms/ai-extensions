---
"radius": patch
---

**Fixed:** In the application graph, an open resource details popover now follows its node card when the layout moves it, instead of staying where the card used to be, and closes when that resource is no longer present in refreshed graph data.
